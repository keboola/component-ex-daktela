{
  "type": "object",
  "title": "Daktela Extractor Configuration",
  "required": [
    "username",
    "#password",
    "from",
    "to",
    "tables"
  ],
  "properties": {
    "username": {
      "type": "string",
      "title": "Username",
      "description": "Daktela account username",
      "propertyOrder": 1
    },
    "#password": {
      "type": "string",
      "title": "Password",
      "format": "password",
      "description": "Daktela account password",
      "propertyOrder": 2
    },
    "url": {
      "type": "string",
      "title": "URL",
      "description": "Full Daktela instance URL (e.g., https://mycompany.daktela.com). Either URL or Server is required.",
      "propertyOrder": 3
    },
    "server": {
      "type": "string",
      "title": "Server",
      "description": "Daktela server name (e.g., mycompany). Will be converted to https://mycompany.daktela.com. Either URL or Server is required.",
      "propertyOrder": 4
    },
    "from": {
      "type": "string",
      "title": "From Date",
      "description": "Start date for data extraction. Supported formats: 'today' or '0' (current time minus 30 minutes), negative integer (e.g., '-7' for 7 days ago), or 'YYYY-MM-DD'.",
      "propertyOrder": 5
    },
    "to": {
      "type": "string",
      "title": "To Date",
      "description": "End date for data extraction. Supported formats: 'today' or '0' (current time minus 30 minutes), negative integer (e.g., '-1' for yesterday), or 'YYYY-MM-DD'.",
      "propertyOrder": 6
    },
    "tables": {
      "type": "array",
      "title": "Tables",
      "description": "Select tables to extract from Daktela. Choose from predefined tables or enter custom table names.",
      "propertyOrder": 7,
      "minItems": 1,
      "uniqueItems": true,
      "format": "select",
      "items": {
        "type": "string",
        "enum": [
          "accounts",
          "accounts_custom_fields",
          "activities",
          "activities_call",
          "activities_chat",
          "activities_email",
          "activities_sms",
          "activities_statuses",
          "calls",
          "campaigns",
          "categories",
          "chats",
          "contacts",
          "contacts_custom_fields",
          "devices",
          "emails",
          "fields",
          "files",
          "forms",
          "groups",
          "holidays",
          "notes",
          "pauses",
          "profiles",
          "queues",
          "recordings",
          "records",
          "schedules",
          "skills",
          "sms",
          "statuses",
          "templates",
          "tickets",
          "tickets_categories",
          "users",
          "users_queues"
        ]
      },
      "options": {
        "tags": true,
        "freeInput": true
      }
    },
    "incremental": {
      "type": "boolean",
      "title": "Incremental Load",
      "description": "If enabled, new data will be appended to existing tables. If disabled, existing data will be replaced.",
      "default": false,
      "propertyOrder": 8
    },
    "debug": {
      "type": "boolean",
      "title": "Debug Mode",
      "description": "Enable verbose logging for debugging purposes.",
      "default": false,
      "propertyOrder": 9
    }
  }
}
